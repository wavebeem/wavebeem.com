// Central inventory of icons used across the site. Import from here, not
// directly from @lucide/astro, so every icon in use is visible in one
// place, and each name is tied to its purpose -- reusing a shape for an
// unrelated purpose means adding a new export, not reaching for an
// existing one that happens to look right.

export { Menu as IconMenu, X as IconClose } from "@lucide/astro";
