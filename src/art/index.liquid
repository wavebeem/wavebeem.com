---
title: "Art"
layout: "layout-base"
---
<article>
  <header>
    <h1>{{ title | escape }}</h1>
    <p>A gallery for my art.</p>
  </header>

  <div class="grid2">
    {% for image in art.images reversed %}
    <figure>
      <a href="{{ image.path }}">
        <img src="{{ image.path }}" alt="" />
      </a>
      <figcaption>
        {{ image.title }}
        <br />
        <small>{% render "partials/date" date: image.date %}</small>
      </figcaption>
    </figure>
    {% endfor %}
  </div>
</article>
