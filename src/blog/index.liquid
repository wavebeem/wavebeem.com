---
title: "Blog"
layout: "layout-page"
override:tags: []
permalink: null
tags:
  - "nav"
---

<p>
  Programming guides, project updates, video game reviews, life events, and
  more.
</p>

<p><a href="/subscribe/">Subscribe</a> to stay up to date.</p>

{% assign postsByYear = collections.posts | groupByYear %} {% for item in
postsByYear %} {% assign year = item[0] %} {% assign postList = item[1] %}

<h2>{{ year }}</h2>

<ul>
  {% for post in postList %}
  <li>
    <p>
      <a href="{{ post.url }}">
        {{ post.data.title | escape }}
      </a>
    </p>
  </li>
  {% endfor %}
  </ul>
{% endfor %}


{{ content }}
