---
layout: "layout-base"
---
<article class="page">
  <header class="page-header">
    <h1>{{ title | escape }}</h1>
    <small>{% render "partials/date" date: page.date %}</small>
  </header>

  {% if draft %}
  <section class="page-body">
    <p class="card">
      <strong>
        This is a draft post. It isn't finished yet. Please don't share this
        page with anyone else without my permission.
      </strong>
    </p>
  </section>
  {% endif %}

  <section class="page-body">
    {% if description -%}
    <p>{{ description | markdownify }}</p>
    {% endif %}
    {{ content }}
  </section>

  <footer class="page-footer">
    <hr />
    <p>
      Thanks for reading.
      <a href="/contact/">Let's talk</a>
      about this post.
      <a href="/subscribe/">Subscribe</a> to stay up to date.
    </p>
  </footer>
</article>

