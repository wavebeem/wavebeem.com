---
layout: "layout-base"
---

<h1>Posts</h1>
<ul>
  {% for post in collections.post reversed %}
  <li>
    <p>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <br />{{ post.date | date: '%B %d, %Y' }}
    </p>
  </li>
  {% endfor %}
</ul>

{{ content }}
