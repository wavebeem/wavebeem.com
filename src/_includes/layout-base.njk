<!DOCTYPE html>
<html
  lang="en"
  class="theme-root sage-scrollbar"
  data-tags="{{ (tags or []) | join(' ') }}"
>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="content-security-policy" content="{{ csp | safe }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content="{{ eleventy.generator }}" />
    <meta name="theme-color" content="{{ colors['color-header-background'] }}"
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-italic-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/ultra-hi-gloss.woff2"
    />
    <meta name="author" content="wavebeem" />
    <meta name="description" content="{{ description }}" />
    <link
      rel="icon"
      sizes="16x16"
      href="/static/favicon.png?v={{ contentHashes.favicon }}"
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/static/photo-180.webp?v={{ contentHashes.photo }}"
    />
    <link
      rel="alternate"
      type="application/atom+xml"
      href="{{ origin }}/feed.xml"
      title="Subscribe to wavebeem.com"
    />
    <meta
      name="og:image"
      content="{{ origin }}/static/og-image.png?v={{ contentHashes.ogImage }}"
    />
    <meta name="og:image:width" content="512" />
    <meta name="og:image:height" content="256" />
    <meta name="og:title" content="{{ title | formatTitle }}" />
    <meta name="og:description" content="{{ description }}" />
    <title>{{ title | formatTitle }}</title>
    <script
      async
      defer
      data-domain="wavebeem.com"
      src="https://plausible.io/js/plausible.js"
    ></script>
    <link rel="stylesheet" media="screen" href="/main.css?t={{ dateNow }}" />
    {% for url in (css or []) %}
    <link
      rel="stylesheet"
      media="screen"
      href="/static/{{ url }}.css?t={{ dateNow }}"
    />
    {% endfor %}
    <link
      rel="stylesheet"
      media="print"
      href="/static/print.css?t={{ dateNow }}"
    />
  </head>
  <body>
    <header class="header theme-root theme-header screen-only">
      <div class="content-column">
        <div class="flex">
          <a href="/" class="logo sage-link">wavebeem </a>
        </div>
        <details class="hamburger">
          <summary>Menu</summary>
          <nav class="nav">
            <a href="/">Home</a>
            <a href="/projects/">Projects</a>
            <a href="/contact/">Contact info</a>
            <a href="/blog/">Blog</a>
            <a href="/art/">Art gallery</a>
            <a href="/resume/">Resume</a>
            <a href="/about/">About this site</a>
            <a href="/uses/">Things I use</a>
            <a href="/links/">Cool links</a>
            <a href="/shrines/">Shrines</a>
          </nav>
        </details>
      </div>
    </header>
    <div class="slime"></div>
    <main>
      <div class="content-column">{{ content | safe }}</div>
    </main>
    <footer class="footer screen-only sage-texture-striped">
      <div class="content-column">
        &copy; {{ "now" | formatDate("yyyy") }} Sage Fennel (<i
          class="sage-bright"
          >wavebeem</i
        >)
      </div>
    </footer>
  </body>
</html>
