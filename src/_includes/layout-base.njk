<!DOCTYPE html>
<html
  lang="en"
  class="candy-root candy-texture-striped"
  data-tags="{{ (tags or []) | join(' ') }}"
>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="content-security-policy" content="{{ csp | safe }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content="{{ eleventy.generator }}" />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-italic-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/ultra-hi-gloss.woff2"
    />
    <meta name="author" content="wavebeem" />
    <meta name="description" content="{{ description }}" />
    <meta name="theme-color" content="oklch(30% 5% 190)" />
    <link
      rel="icon"
      sizes="16x16"
      href="/static/favicon.png?v={{ faviconVersion }}"
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/static/photo-180.webp?v={{ photoVersion }}"
    />
    <link
      rel="alternate"
      type="application/atom+xml"
      href="https://www.wavebeem.com/feed.xml"
      title="Subscribe to wavebeem.com"
    />
    <meta
      name="og:image"
      content="https://www.wavebeem.com/static/photo-512.webp?v={{
        photoVersion
      }}"
    />
    <meta name="og:image:width" content="512" />
    <meta name="og:image:height" content="512" />
    <meta name="og:title" content="{{ title | formatTitle }}" />
    <meta name="og:description" content="{{ description }}" />
    <title>{{ title | formatTitle }}</title>
    <script
      async
      defer
      data-domain="wavebeem.com"
      src="https://plausible.io/js/plausible.js"
    ></script>
    <link
      rel="stylesheet"
      media="screen"
      href="/static/candy.css?t={{ dateNow }}"
    />
    <link
      rel="stylesheet"
      media="screen"
      href="/static/global.css?t={{ dateNow }}"
    />
    {% for url in (css or []) %}
    <link
      rel="stylesheet"
      media="screen"
      href="/static/{{ url }}.css?t={{ dateNow }}"
    />
    {% endfor %}
    <link
      rel="stylesheet"
      media="print"
      href="/static/print.css?t={{ dateNow }}"
    />
  </head>
  <body class="candy-root">
    <header class="screen-only">
      <a href="/" class="flex align-center logo candy-link"
        >wavebeem<span aria-hidden="true">&nbsp;*-</span>
      </a>
      <nav>
        <a class="candy-link" href="/">Home</a>
        <a class="candy-link" href="/blog/">Blog</a>
        <a class="candy-link" href="/art/">Art</a>
        <a class="candy-link" href="/resume/">Resume</a>
      </nav>
    </header>
    <main class="candy-root candy-card candy-texture-paper">
      {{ content | safe }}
    </main>
    <footer class="footer screen-only">
      &copy; {{ "now" | formatDate("yyyy") }} Sage Fennel (<i
        class="candy-bright"
        >wavebeem</i
      >)
    </footer>
  </body>
</html>
