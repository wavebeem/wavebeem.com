<!DOCTYPE html>
<html
  lang="en"
  class="sage-root sage-scrollbar"
  data-tags="{{ (tags or []) | join(' ') }}"
>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="content-security-policy" content="{{ csp | safe }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content="{{ eleventy.generator }}" />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/nunito-italic-variable.woff2"
    />
    <link
      rel="preload"
      as="font"
      crossorigin="anonymous"
      href="/static/fonts/ultra-hi-gloss.woff2"
    />
    <meta name="author" content="wavebeem" />
    <meta name="description" content="{{ description }}" />
    <link
      rel="icon"
      sizes="16x16"
      href="/static/favicon.png?v={{ contentHashes.favicon }}"
    />
    <link
      rel="apple-touch-icon"
      sizes="180x180"
      href="/static/photo-180.webp?v={{ contentHashes.photo }}"
    />
    <link
      rel="alternate"
      type="application/atom+xml"
      href="https://www.wavebeem.com/feed.xml"
      title="Subscribe to wavebeem.com"
    />
    <meta
      name="og:image"
      content="https://www.wavebeem.com/static/photo-512.webp?v={{
        contentHashes.photo
      }}"
    />
    <meta name="og:image:width" content="512" />
    <meta name="og:image:height" content="512" />
    <meta name="og:title" content="{{ title | formatTitle }}" />
    <meta name="og:description" content="{{ description }}" />
    <title>{{ title | formatTitle }}</title>
    <script
      async
      defer
      data-domain="wavebeem.com"
      src="https://plausible.io/js/plausible.js"
    ></script>
    <link rel="stylesheet" media="screen" href="/main.css?t={{ dateNow }}" />
    {% for url in (css or []) %}
    <link
      rel="stylesheet"
      media="screen"
      href="/static/{{ url }}.css?t={{ dateNow }}"
    />
    {% endfor %}
    <link
      rel="stylesheet"
      media="print"
      href="/static/print.css?t={{ dateNow }}"
    />
  </head>
  <body>
    <header class="screen-only sage-root theme-header">
      <div class="content-column">
        <div class="flex">
          <a href="/" class="logo sage-link">wavebeem </a>
        </div>
        <nav class="window-nav screen-only">
          <a class="sage-link" href="/">Home</a>
          <a class="sage-link" href="/blog/">Blog</a>
          <a class="sage-link" href="/art/">Art</a>
          <a class="sage-link" href="/resume/">Resume</a>
        </nav>
      </div>
    </header>
    <main>
      <div class="warning-stripe sage-texture-striped"></div>
      <div class="content-column">{{ content | safe }}</div>
    </main>
    <footer class="footer screen-only sage-texture-striped">
      <div class="content-column">
        &copy; {{ "now" | formatDate("yyyy") }} Sage Fennel (<i
          class="sage-bright"
          >wavebeem</i
        >)
      </div>
    </footer>
  </body>
</html>
