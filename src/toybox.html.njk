---
title: "Toybox"
description: |
  A silly little collection of toys and jokes made specifically for the web.
layout: "layout-base"
permalink: "/toybox/"
css:
  - "blog"
---

<h1>{{ title }}</h1>

<p>{{ description }}</p>

<div class="post-list">
  {% for post in collections.toybox | reverse %}
  <section class="post-list--post sage-well">
    <h2>
      <a class="sage-link no-underline" href="{{ post.url }}">
        {{ post.data.title }}
      </a>
    </h2>

    <p>
      {{ post.data.description }}
    </p>

    <time
      datetime="{{ post.date | formatDate('yyyy-mm-dd') }}"
      >{{ post.date | formatDate('yyyy mmmm d') }}</time
    >
  </section>
  {% endfor %}
</div>

{{ content | safe }}
